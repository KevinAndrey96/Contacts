import { Title } from '@angular/platform-browser';

export interface User {
    firstName?: string,
    lastName?: string,
    documentType?: string,
    documentNumber?: string,
    phone?: string,
    email?: string,
    password?: string,
};


